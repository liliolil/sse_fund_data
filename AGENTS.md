# Project Instructions

这是一个用于采集上海证券交易所基金公开数据的项目。

## 项目目标

建立一个稳定、可维护、可增量更新的基金公开数据采集系统。

主要研究和采集：

- ETF 规模
- LOF 规模
- 基金成交数据
- 基金公告
- ETF 申购赎回清单 PCF
- 基金 XBRL 数据
- 其他具有研究价值的基金公开数据

## 开发原则

1. 优先调查真实数据来源。
2. 优先使用 requests/httpx 和公开接口。
3. 只有在无法稳定通过 HTTP 请求获取数据时才使用 Selenium。
4. 不允许猜测不存在的 API。
5. 不允许编造字段或模拟真实数据。
6. 如果某个字段或接口没有验证，应明确标注“未验证”。
7. 原始数据和清洗后的数据分开保存。
8. 不要写死页数、基金数量或最新日期。
9. 所有抓取逻辑都要考虑历史回填和增量更新。
10. 请求频率保持合理，不进行高并发抓取。

## 代码要求

- Python 为主要开发语言。
- 代码优先考虑可读性和可维护性。
- 不使用过度复杂的设计模式。
- 关键逻辑使用适量中文注释。
- 不要把所有逻辑写进 main.py。
- 每个数据源尽量独立成模块。
- 对异常进行明确处理，不要静默忽略。
- 不要大量使用 time.sleep。
- Selenium 如必须使用，应使用 WebDriverWait。

## 修改规则

在修改现有代码前：

1. 先阅读相关文件。
2. 明确当前实现。
3. 尽量只修改与当前任务有关的文件。
4. 如果必须修改任务范围之外的文件，先说明原因。
5. 不要顺手做无关重构。

## 验证要求

每完成一个功能：

1. 实际运行代码。
2. 检查返回数据是否真实。
3. 检查结果是否为空。
4. 检查是否存在明显重复。
5. 如有测试，运行相关测试。
6. 失败时应先定位原因，再修改。
7. 不要仅凭“代码没有报错”判断任务完成。

## 调研规则

调研网页数据源时：

1. 区分 HTML、XHR、Fetch、JSON、JSONP、文件下载和 Selenium。
2. 优先寻找页面真实网络请求。
3. 对 API 记录：
   - URL
   - 请求方法
   - 参数
   - 返回格式
   - 分页机制
   - 日期参数
4. 不确定的信息必须明确说明。
5. 不要根据接口名称猜测字段含义。

## Codex 工作方式

用户当前主要是在学习 Codex 并使用 Codex 完成实际项目。

因此：

- 重点解释做了什么和为什么这么做。
- 不需要逐行讲解基础 Python。
- 复杂任务应分阶段完成。
- 一个阶段完成后应停止，等待用户确认。
- 不要未经要求自动扩展任务范围。
